<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="sublimePickerStyle">@style/MySublimePickerStyle</item>

    </style>

    <style name="ActivityTheme" parent="AppTheme"></style>

    <style name="ActivityTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--自定义日期选择器的样式-->
    <style name="MySublimePickerStyle" parent="SublimePickerStyleLight">
        <item name="colorAccent">@color/blue</item>
        <item name="colorPrimaryDark">@color/blue</item>
        <item name="colorPrimary">@color/blue</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/grey</item>
        <item name="android:textColorPrimaryInverse">@color/white</item>
    </style>

    <style name="ActivityTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="ActivityTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
